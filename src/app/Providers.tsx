'use client';import{useEffect,useRef}from'react';import{usePathname}from'next/navigation';import{DataProvider,useData}from'@/contexts/DataContext';import{AuthProvider,useAuth}from'@/contexts/AuthContext';import{ToastProvider}from'@/components/ui/Toast';import Layout from'@/components/icons/Layout';import{PWAInstaller}from'@/components/PWAInstaller';import{registerServiceWorker}from'@/utils/pwa';import{notifyAdmin}from'@/utils/adminNotify';
const PUBLIC_PATHS=['/','/agence','/mannequins','/fashion-day','/magazine','/services','/casting','/contact'];
function RuntimeEffects(){const pathname=usePathname();const{data}=useData();const{user}=useAuth();const notified=useRef(new Set<string>());useEffect(()=>{registerServiceWorker()},[]);useEffect(()=>{if(!pathname||!PUBLIC_PATHS.some(p=>pathname===p||pathname.startsWith(`${p}/`))||notified.current.has(pathname))return;notified.current.add(pathname);notifyAdmin('visit',`Page visitée : ${pathname==='/'?'Accueil':pathname.replace(/^\//,'').replace(/-/g,' ')}`).catch(()=>{})},[pathname]);useEffect(()=>{if(!data||!pathname?.startsWith('/admin'))return;const n=(data.castingApplications?.filter((i:any)=>i.status==='Nouveau').length||0)+(data.fashionDayApplications?.filter((i:any)=>i.status==='Nouveau').length||0)+(data.recoveryRequests?.filter((i:any)=>i.status==='Nouveau').length||0)+(data.bookingRequests?.filter((i:any)=>i.status==='Nouveau').length||0)+(data.contactMessages?.filter((i:any)=>i.status==='Nouveau').length||0);document.title=n?`(${n}) Admin | Perfect Models Management`:'Admin | Perfect Models Management'},[pathname,data]);useEffect(()=>{if(!data?.apiKeys?.dropboxAccessToken)return;import('@/utils/dropboxService').then(({dropboxService})=>dropboxService.updateToken(data.apiKeys.dropboxAccessToken!))},[data?.apiKeys?.dropboxAccessToken]);return null}
export default function Providers({children}:{children:React.ReactNode}){return <DataProvider><AuthProvider><ToastProvider><RuntimeEffects/><Layout>{children}</Layout><PWAInstaller/></ToastProvider></AuthProvider></DataProvider>}
