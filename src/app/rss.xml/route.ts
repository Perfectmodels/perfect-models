import{collectionToArray,getCollection}from'@/lib/app-data';const BASE='https://perfectmodels.online';const esc=(v:string)=>String(v||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&apos;');export async function GET(){const articles=collectionToArray(await getCollection('articles')).slice(0,30);const items=articles.map((a:any)=>{const slug=a.slug||a.id,url=`${BASE}/magazine/${slug}`,date=new Date(a.createdAt||a.date||Date.now()).toUTCString();return`<item><title>${esc(a.title||'Article')}</title><link>${url}</link><guid isPermaLink="true">${url}</guid><pubDate>${date}</pubDate><description>${esc(a.excerpt||'')}</description></item>`}).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>Perfect Models Management - Magazine</title><link>${BASE}/magazine</link><description>Actualités et tendances mode au Gabon.</description><language>fr</language><lastBuildDate>${new Date().toUTCString()}</lastBuildDate>${items}</channel></rss>`;return new Response(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8','Cache-Control':'public, max-age=1800, s-maxage=3600'}})}
