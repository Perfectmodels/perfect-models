import { NextResponse } from 'next/server';
import { sqlQuery } from '@/lib/neon';
interface Row{login_email:string;identifier:string;app_role:string;status:string;name:string}
const ADMIN_ALIASES=new Set(['admin','contact@perfectmodels.online','contact@perfectmodels.ga','perfectmodels.ga@gmail.com']);
export async function POST(request:Request){const body=await request.json().catch(()=>({}));let identifier=String(body.identifier||'').trim().toLowerCase();if(!identifier)return NextResponse.json({error:'Identifiant requis.'},{status:400});if(ADMIN_ALIASES.has(identifier))identifier='admin';const rows=await sqlQuery<Row>(`SELECT ap.login_email,ap.identifier,ap.app_role,ap.status,u.name FROM public.auth_profiles ap JOIN neon_auth."user" u ON u.id=ap.user_id WHERE lower(ap.identifier)=lower($1) OR lower(ap.login_email)=lower($1) OR lower(u.name)=lower($1) ORDER BY CASE WHEN lower(ap.identifier)=lower($1) THEN 0 WHEN lower(ap.login_email)=lower($1) THEN 1 ELSE 2 END LIMIT 1`,[identifier]);const row=rows[0];if(!row||row.status!=='active')return NextResponse.json({error:'Identifiant introuvable ou compte inactif.'},{status:404});return NextResponse.json({email:row.login_email,identifier:row.identifier,role:row.app_role,name:row.name});}
