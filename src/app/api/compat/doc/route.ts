import { NextRequest,NextResponse } from 'next/server';
import { getCurrentAppProfile } from '@/lib/auth/profile';
import { getSql } from '@/lib/neon';
import { getCollection,setCollection } from '@/lib/app-data';
async function account(path:string){const[,id]=path.split('/');const sql=getSql();const rows=await sql.query(`SELECT ap.user_id::text uid,ap.login_email email,ap.app_role role,ap.profile_id "profileId",ap.identifier username,ap.permissions,ap.status,u.name "displayName" FROM public.auth_profiles ap JOIN neon_auth."user" u ON u.id=ap.user_id WHERE ap.user_id::text=$1 LIMIT 1`,[id]) as any[];return rows[0]||null}
export async function GET(r:NextRequest){const p=await getCurrentAppProfile();if(!p)return NextResponse.json({error:'Non autorisé.'},{status:401});const path=r.nextUrl.searchParams.get('path')||'';if(path.startsWith('accounts/'))return NextResponse.json({data:await account(path)});const docs=(await getCollection('legacyDocuments')) as Record<string,unknown>|null;return NextResponse.json({data:docs?.[path]??null})}
export async function PUT(r:NextRequest){const p=await getCurrentAppProfile();if(!p)return NextResponse.json({error:'Non autorisé.'},{status:401});const b=await r.json().catch(()=>({}));const path=String(b.path||'');const data=b.data&&typeof b.data==='object'?b.data:{};if(path.startsWith('accounts/')){if(p.role!=='admin')return NextResponse.json({error:'Admin requis.'},{status:403});const[,id]=path.split('/');const sql=getSql();if(data.permissions)await sql.query('UPDATE public.auth_profiles SET permissions=$1::jsonb,updated_at=now() WHERE user_id::text=$2',[JSON.stringify(data.permissions),id]);return NextResponse.json({success:true})}const docs=((await getCollection('legacyDocuments'))||{}) as Record<string,unknown>;docs[path]=b.merge&&docs[path]&&typeof docs[path]==='object'?{...(docs[path] as object),...data}:data;await setCollection('legacyDocuments',docs,p.userId,'compat-write');return NextResponse.json({success:true})}
export async function DELETE(r:NextRequest){const p=await getCurrentAppProfile();if(!p||p.role!=='admin')return NextResponse.json({error:'Admin requis.'},{status:403});const path=r.nextUrl.searchParams.get('path')||'';const docs=((await getCollection('legacyDocuments'))||{}) as Record<string,unknown>;delete docs[path];await setCollection('legacyDocuments',docs,p.userId,'compat-delete');return NextResponse.json({success:true})}
